import * as React from 'react'
import Slide from "../topicos/Slide"

class INTRO extends Slide {

  // tslint:disable-next-line:prefer-function-over-method
  public render() {
    return (
      <div>
        intro slide, no sub title
      </div>
    )
  }
}

export default INTRO